export interface BlogCardProps {
  title: string;
  description: string;
  date: string;
  slug: string;
}

export interface Frontmatter {
  title: string;
  date: string;
  description: string;
  banner: string;
  bannerAlt: string;
}
